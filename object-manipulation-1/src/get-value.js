/* exported getValue */

// create storage for output and set it equal to the value of the property in an object
// return output storage

function getValue(object, key) {
  var value = object[key];
  return value;
}
