/* exported getLastChar */

// create storage for output and set it equal to the last character in the string
// return the output storage

function getLastChar(string) {
  var lastChar = string[string.length - 1];
  return lastChar;
}
