/* exported last */

// way to start work
//   return the value that is last in your list by taking the length of your of your list and subtracting one

function last(array) {
  return array[array.length - 1];
}
